export enum SetConfig {
    APPLICATION = 'app',
    BAUD = 'baud',
    ECHO = 'echo',
    VERBOSE = 'verbose',
    PORT = 'aserver_port', // @TODO: test if this is seperated by a `_` or `&`
    PROMPT = 'aserver_prompt', // @TODO: test if this is seperated by a `_` or `&`
    DEGREE_UNITS = 'deg',
}